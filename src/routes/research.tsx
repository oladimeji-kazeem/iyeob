import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowRight, BookOpen, FlaskConical, Scale } from "lucide-react";
import { AppLayout } from "@/components/app-layout";
import { Button } from "@/components/ui/button";

export const Route = createFileRoute("/research")({ head: () => ({ meta: [{ title: "Research with Synthetic Data | IYEOB" }, { name: "description", content: "Responsible guidance for using IYEOB synthetic datasets in African AI research." }, { property: "og:title", content: "Research with Synthetic Data | IYEOB" }, { property: "og:description", content: "Responsible guidance for using IYEOB synthetic datasets in African AI research." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: ResearchPage });
function ResearchPage() { return <AppLayout><section className="mx-auto max-w-7xl px-5 py-20 lg:px-8"><span className="text-xs font-extrabold uppercase tracking-[0.18em] text-primary">Responsible research</span><h1 className="mt-3 max-w-3xl font-display text-5xl font-extrabold">Use synthetic data with clarity, not false certainty.</h1><p className="mt-6 max-w-2xl text-lg leading-8 text-muted-foreground">IYEOB datasets help teams prototype and teach safely. They are designed scenarios—not government statistics, survey findings, or empirical observations.</p><div className="mt-14 grid gap-5 md:grid-cols-3">{[[FlaskConical,"Transparent methods","Every release documents its generator, assumptions, constraints, and validation approach."],[Scale,"Bounded claims","Quality scores assess construction quality, not real-world representativeness."],[BookOpen,"Reproducible study","Versioned citations make classroom and research use traceable."]].map(([Icon,title,copy]) => { const I=Icon as typeof FlaskConical; return <article className="border border-border bg-card p-7" key={title as string}><I className="size-6 text-primary"/><h2 className="mt-8 font-display text-xl font-bold">{title as string}</h2><p className="mt-3 text-sm leading-6 text-muted-foreground">{copy as string}</p></article> })}</div><Button className="mt-10" asChild><Link to="/datasets">Browse research-ready data <ArrowRight/></Link></Button></section></AppLayout> }